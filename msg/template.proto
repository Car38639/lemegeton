syntax = "proto3";

package template;

service Service {
  rpc REQUEST (MESSAGE) returns (RESPONSE);
}

message MESSAGE {
  string name = 1;
  int32 age = 2;
}

message RESPONSE {
  string message = 1;
}
